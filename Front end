import React from "react";
import { View, Text, Button, Linking, StyleSheet } from "react-native";

export default function App() {
  // 🔗 Replace this with your actual Stripe Checkout link
  const stripeLink = "https://buy.stripe.com/test_123456";

  const handleUpgrade = () => {
    Linking.openURL(stripeLink); // Opens checkout in browser
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>💖 Welcome to My Dating App 💖</Text>
      <Text style={styles.subtitle}>
        Find your match and unlock premium features!
      </Text>

      <View style={styles.buttonWrapper}>
        <Button title="Upgrade to Premium" onPress={handleUpgrade} />
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
    padding: 20,
  },
  title: {
    fontSize: 26,
    fontWeight: "bold",
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 16,
    color: "#555",
    marginBottom: 30,
    textAlign: "center",
  },
  buttonWrapper: {
    width: "80%",
    marginTop: 10,
  },
});
